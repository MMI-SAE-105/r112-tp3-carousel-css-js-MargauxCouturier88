// *** CAROUSEL ***
// TODO


carousel = document.querySelector("carousel__container");
carousel.scrollRight = 520;
carousel.scrollBy({ left: +260, behavior: "smooth" });
carousel.scrollBy({ left: -260, behavior: "smooth" });
prevButton = scrollBy;
nextButton = scrollBy;

const carousel = carousel__container;
const prevButton = carousel__container;
const nextButton = carousel__container;

prevButton.addEventListener("click", () => {
    carousel__container;
  });
       
  nextButton.addEventListener("click", () => {
    carousel__container;
  });
}

premierItem = document.querySelector(".carousel__item");
scrollAmount = premierItem.clientWidth;